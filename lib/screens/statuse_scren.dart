import 'package:flutter/material.dart';

class StatuseScren extends StatelessWidget {
  const StatuseScren({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("no status yet..."),),
    );
  }
}